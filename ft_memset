#include "libft.h"

void *ft_memset(void *memset, int *value, size_t len){
    unsigned char *str;
    size_t i;

    str == memset;
    i = 0;
    while (i < len){
        str[i] = (unsigned char)value;
        i++;
    }
   return (memset);

}